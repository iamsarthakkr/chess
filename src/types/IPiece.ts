export enum PieceType {
   NONE = 0,
   PAWN = 1,
   KNIGHT = 2,
   BISHOP = 3,
   ROOK = 4,
   QUEEN = 5,
   KING = 6,
}

export enum Side {
   BLACK = 0,
   WHITE = 8,
}

export enum Symbols {
   NONE = "_",
   PAWN = "p",
   KNIGHT = "n",
   BISHOP = "b",
   ROOK = "r",
   QUEEN = "q",
   KING = "k",
}
